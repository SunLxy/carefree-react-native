import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  btnItem: {},
  btn: {
    borderWidth: 0.5,
    borderColor: '#ccc',
    borderRadius: 5,
    alignItems: 'center',
  },
  btnText: {
    fontSize: 16,
    color: '#000',
    paddingVertical: 5,
  },
})
