import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  btnItem: {},
  btn: {
    borderRadius: 5,
    alignItems: 'center',
  },
  bordered: {
    borderWidth: 0.5,
    borderColor: '#ccc',
  },
  btnText: {
    fontSize: 16,
    color: '#000',
    paddingVertical: 5,
  },
})
