import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    marginHorizontal: 10,
    marginVertical: 10,
  },
  border: {
    borderWidth: 0.5,
    borderColor: '#ccc',
  },
})
